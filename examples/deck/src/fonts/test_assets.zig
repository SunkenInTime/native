/// Real registered-face fixture shared by the framework's reference-renderer
/// tests. Keeping the bytes beside the deck asset proves the public bundled
/// font path rather than a synthetic outline assembled only for tests.
pub const geist_pixel_square = @embedFile("GeistPixel-Square.ttf");
